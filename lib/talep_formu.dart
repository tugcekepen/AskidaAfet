import 'package:flutter/material.dart';

class TalepFormu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Talep Formu'),
      ),
      body: Center(
        child: Text('Talep Formu İçeriği'),
      ),
    );
  }
}
